structdef MYS {
    float *f;
};

function : int * & foo (int * x) {
    return x;
}

function : int main() {
    int i = 0;
    int * iptr = &i;
    int ********* x = nullptr;
    int * y = ******** x;
    int ** z [5];
    MYS * ptr;
    float f = *(ptr->f) + *y;
    float ** fptr;
    if( (*fptr != nullptr) && ((*ptr).f == *fptr)) {}
    return y[-5];
}